/** Copperline Atelier cart: transparent totals, code validation, and a calm route into checkout. */
import { ArrowRight, Minus, Plus, Trash2 } from "lucide-react";
import { useState } from "react";
import { Link } from "wouter";
import { toast } from "sonner";
import { StorefrontShell } from "@/components/StorefrontShell";
import { useStore } from "@/contexts/StoreContext";
import { formatILS } from "@/lib/money";

export default function Cart() {
  const { state, cartSubtotal, updateQuantity, removeFromCart, setCouponCode, validateCoupon } = useStore();
  const [code, setCode] = useState(state.couponCode ?? "");
  const coupon = validateCoupon(state.couponCode);
  const lines = state.cart.map((line) => ({ ...line, product: state.products.find((item) => item.id === line.productId)! })).filter((line) => line.product);
  const apply = () => { const result = validateCoupon(code); if (result.valid) { setCouponCode(code || null); toast.success(result.message); } else toast.error(result.message); };
  const shipping = coupon.freeShipping || cartSubtotal >= 300 ? 0 : 18;
  return <StorefrontShell><main className="container py-10 md:py-16"><p className="eyebrow">The bag</p><h1 className="mt-3 text-5xl tracking-[-0.055em] md:text-6xl">Your counter, gathered.</h1>{!lines.length ? <div className="mt-9 grid overflow-hidden border border-[#E6D7C7] md:grid-cols-[1.05fr_0.95fr]"><section className="bg-[#FFFDF9] p-10 md:p-14"><p className="eyebrow">01 / Make room</p><h2 className="mt-4 max-w-md text-4xl leading-[0.95]">The bag is still clear.</h2><p className="mt-4 max-w-sm text-sm leading-6 text-[#73675E]">The best counters begin with a little space and one tool with a job to do.</p><Link href="/shop" className="ink-button mt-7">Browse the tool room</Link></section><aside className="relative min-h-64 overflow-hidden bg-[#17130F] p-10 text-[#FAF6F0]"><span className="absolute -right-10 -top-16 font-['Fraunces'] text-[13rem] leading-none text-[#C0632D]/15">01</span><div className="relative border-l border-[#D9A441] pl-5"><p className="text-[10px] font-bold uppercase tracking-[0.17em] text-[#D9A441]">Counter note</p><p className="mt-4 max-w-xs font-['Fraunces'] text-2xl leading-[1.05]">“A smaller collection makes for a better kitchen.”</p><p className="mt-5 text-xs leading-5 text-[#CDBFB2]">Delivery becomes complimentary once the counter reaches {formatILS(300)}.</p></div></aside></div> : <div className="mt-9 grid gap-9 lg:grid-cols-[1fr_360px]"><section>{lines.map(({ product, ...line }) => <article key={`${line.productId}-${line.color}`} className="grid grid-cols-[100px_1fr] gap-5 border-b border-[#E6D7C7] py-5 sm:grid-cols-[130px_1fr_auto]"><img src={product.image} alt={product.name} className="h-28 w-full object-cover sm:h-32" /><div><p className="eyebrow">{product.brand}</p><h2 className="mt-1 text-2xl">{product.name}</h2><p className="mt-2 text-sm text-[#73675E]">Finish: {line.color}</p><div className="mt-4 flex items-center gap-3"><div className="flex items-center border border-[#D7C6B6]"><button onClick={() => updateQuantity(line.productId, line.color, line.quantity - 1)} className="p-2"><Minus size={14} /></button><span className="w-7 text-center text-xs font-bold">{line.quantity}</span><button onClick={() => updateQuantity(line.productId, line.color, line.quantity + 1)} className="p-2"><Plus size={14} /></button></div><button onClick={() => removeFromCart(line.productId, line.color)} className="inline-flex items-center gap-1 text-[10px] font-bold uppercase tracking-[0.12em] text-[#8B7D70] hover:text-[#C0632D]"><Trash2 size={13} /> Remove</button></div></div><b className="col-start-2 self-start text-right text-lg sm:col-start-auto">{formatILS(product.price * line.quantity)}</b></article>)}</section><aside className="paper-panel h-fit p-6"><p className="eyebrow">Order notes</p><h2 className="mt-2 text-3xl">The useful math.</h2><div className="mt-6 flex border-b border-[#BFAE9E] pb-2"><input value={code} onChange={(event) => setCode(event.target.value.toUpperCase())} placeholder="COPPER10" className="min-w-0 flex-1 bg-transparent text-sm uppercase outline-none" /><button onClick={apply} className="text-[10px] font-bold uppercase tracking-[0.14em] text-[#8A4A27]">Apply</button></div>{state.couponCode && <p className={`mt-2 text-xs ${coupon.valid ? "text-[#687C5D]" : "text-[#B5412F]"}`}>{coupon.message}</p>}<div className="mt-6 grid gap-3 border-y border-[#E6D7C7] py-5 text-sm"><div className="flex justify-between"><span>Subtotal</span><span>{formatILS(cartSubtotal)}</span></div><div className="flex justify-between"><span>Delivery</span><span>{shipping ? formatILS(shipping) : "On us"}</span></div>{coupon.discount > 0 && <div className="flex justify-between text-[#8A4A27]"><span>Copper saving</span><span>−{formatILS(coupon.discount)}</span></div>}</div><div className="mt-5 flex items-end justify-between"><span className="text-xs font-bold uppercase tracking-[0.14em]">Total</span><b className="font-['Fraunces'] text-3xl">{formatILS(cartSubtotal + shipping - coupon.discount)}</b></div><Link href="/checkout" className="copper-button mt-6 w-full">Go to checkout <ArrowRight size={16} /></Link></aside></div>}</main></StorefrontShell>;
}
